public class ReplaceCharacter {

        public static String replaceChar(String st){

        String s= st.replace('d', 'f');// Replace all the 'd' characters with 'f' characters.
        String  str= s.replace('l', 't');// Replace all the 'l' characters with 't' characters.
        return str;
    }
}

